<div class="container">

  <div class="container">
  <h1>How it works</h1>
  <ul>
    <li>Create an account or login below</li>
    <li>Access your dashboard </li>
    <li>Create your portfolio </li>
    <li>Search and Add coins to your portfolio </li>
    <li>Then create your transactions (not yet available)</li>
  </ul>
</div>

<h2>Log in</h2>

  <%= simple_form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
    <div class="form-inputs">
      <%= f.input :email,
                  required: false,
                  autofocus: true,
                  input_html: { autocomplete: "email" } %>
      <%= f.input :password,
                  required: false,
                  input_html: { autocomplete: "current-password" } %>
      <%= f.input :remember_me, as: :boolean if devise_mapping.rememberable? %>
    </div>

    <div class="form-actions">
      <%= f.button :submit, "Log in" %>
    </div>
  <% end %>

  <%= render "devise/shared/links" %>
  <div class="right">
    <strong> Data provided by </strong>
    <%=image_tag "https://static.coingecko.com/s/coingecko-branding-guide-8447de673439420efa0ab1e0e03a1f8b0137270fbc9c0b7c086ee284bd417fa1.png", class: 'brand'%>
  </div>
</div>
